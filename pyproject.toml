[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hr-ai-advisory"
version = "0.4.5"
description = "AI-powered HR advisory platform for Singapore SMEs"
authors = [
    {name = "Terrene Foundation", email = "dev@arbor.sg"}
]
readme = "README.md"
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    # Kailash SDK (includes: fastapi, uvicorn, pydantic, sqlalchemy, pandas, numpy, requests, pyyaml)
    "kailash>=2.8.9",
    "kailash-nexus>=2.1.1",
    "kailash-dataflow>=2.0.12",
    "kailash-kaizen>=2.7.5",
    "kailash-pact>=0.8.2",
    "kailash-mcp>=0.2.5",
    "kaizen-agents>=0.9.3",
    # Terrene Foundation trust frameworks (bundled in src/ — not on PyPI)
    # "eatp>=0.2.0",
    # "trust-plane>=0.2.1",
    # CLI Framework
    "click>=8.0",
    "rich>=13.0.0",
    # Database migrations
    "alembic>=1.12.0",
    # Utilities
    "python-dotenv>=1.0.0",
    "structlog>=23.1.0",
    # pgvector support
    "pgvector>=0.3.0",
    # Document generation
    "python-docx>=1.1.0",
    # Email
    "sendgrid>=6.11.0",
    # JWT auth
    "PyJWT>=2.8.0",
    "passlib[bcrypt]>=1.7.4",
    # PostgreSQL driver
    "psycopg2-binary>=2.9.9",
    # LLM provider (required by Kaizen agents for response generation)
    "openai>=1.30.0",
    # Async HTTP client (MCP integration adapters)
    "httpx>=0.27.0",
    # Encryption (OAuth token store, PII encryption)
    "cryptography>=42.0.0",
    # Kailash runtime transitive deps (not declared by kailash core on PyPI)
    "aiohttp>=3.9.0",
    "mcp>=1.0.0",
    "psutil>=5.9.0",
    "tiktoken>=0.7.0",
    "python-multipart>=0.0.7",
    "email-validator>=2.0.0",
]

[project.optional-dependencies]
ml = [
    # PyTorch - On Windows, install separately: pip install torch --index-url https://download.pytorch.org/whl/cu118
    "torch>=2.7.1",
]
dev = [
    # Testing (pytest>=7.2.0 removes vulnerable py dependency)
    "pytest>=7.2.0",
    "pytest-cov>=4.0",
    "pytest-asyncio>=0.21.0",
    # Linting & Formatting
    "black>=23.0",
    "isort>=5.12.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
    # Security & Pre-commit
    "detect-secrets>=1.5.0",
    "pre-commit>=4.2.0",
    # Load testing
    "locust>=2.29",
]

[project.urls]
"Homepage" = "https://github.com/terrene-foundation/arbor"
"Bug Tracker" = "https://github.com/terrene-foundation/arbor/issues"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests*", ".*", "__pycache__*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning:kaizen.*:",
    "ignore::DeprecationWarning:jwt.*:",
    "ignore::DeprecationWarning:passlib.*:",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests requiring external services",
    "requires_postgres: marks tests that need a running PostgreSQL instance",
    "e2e: marks tests as end-to-end tests exercising full user journeys",
    "regression: bug regression test (never delete)",
]

[tool.black]
line-length = 100
target-version = ["py311", "py312", "py313"]

[tool.isort]
profile = "black"
line_length = 100

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_ignores = true
ignore_missing_imports = true
