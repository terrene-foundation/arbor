timestamp: 2026-04-29T00:00:00Z
source: kailash-coc-claude-py (cache @ ~/.cache/kailash-coc/, refreshed via fetch+reset origin/main)
template_version: 3.5.29
previous_template_version: 3.5.25
variant: py
type: coc-project (downstream)
notes: |
  Downstream /sync 3.5.25 -> 3.5.29 (4 template versions).
  Cache was force-updated (loom 2.10.0 commit 3a91b79).

  Modified rules (4):
    rules/zero-tolerance.md   (Rule 2 'Fake dispatch' bullet + new Rule 3c + new Rule 6a)
    rules/schema-migration.md (new Rule 1a — /redteam grep for inline DDL)
    rules/agents.md           (new MUST sub-clause — parallel brief-claim verification ≥3 issues)
    rules/git.md              (in-flight from prior 2.9.7)

  Modified other:
    commands/sync.md          (resolver protocol updates)
    .coc-sync-marker          (loom-side stats — overwritten with downstream notes)

  Obsoleted purged: 0 (already clean).
  No new files added this sync.
  No new commands.

  SDK pin bumps:
    kailash         2.11.3 -> 2.12.0  (minor)
    kailash-dataflow 2.3.2 -> 2.4.0   (minor)
    kailash-ml      1.5.1  -> 1.6.0   (minor; tracked in VERSION map only — not pinned, Arbor doesn't import)
    Others unchanged: nexus 2.3.0, kaizen 2.13.1, pact 0.11.0, mcp 0.2.10, kaizen-agents 0.9.4

  uv sync clean (3 packages reinstalled). uv pip check clean (148 pkgs).
  Hooks: 14 scripts in .claude/hooks/, 0 scripts/hooks/ refs in settings.json.
