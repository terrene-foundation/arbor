# Deployment: 2026-03-14 v1.1.0

> **Historical record (annotated 2026-05-07).** This file references `https://arbor.terrene.foundation` — that domain never resolved per project owner (2026-05-07). The **commits + functional changes** are authoritative; the **deploy mechanics + URL** describe an environment that does not match current reality. Current deploy reality lives in `deploy/deployment-config.md` and `.claude/skills/project/k8s-deploy.md`.

## Summary

Deploy M10-M12 UX improvements and red team fixes to production.

## Changes Deployed

### Security Fixes (from Red Team Round 6)

- **Tenant isolation**: Conversation endpoints now verify ownership via `_conversation_owners` mapping
- **SSE token refresh**: 401 retry with automatic `refreshAccessToken()` in streaming client
- **CSV injection prevention**: `sanitizeCsvCell()` sanitizes dangerous formula characters in exports
- **Thread-safe escalation counter**: `itertools.count(1)` replaces non-atomic global int

### UX Improvements (M10-M12)

- Typography scale with accessibility text size multiplier
- `prefers-reduced-motion` media query for WCAG compliance
- WCAG AA contrast compliance (gray-400 -> gray-500 for disclaimers)
- Missing CSS variables (`--color-primary-bg`, `--shadow-dropdown`) defined
- SearchResults keyboard navigation performance (useMemo)
- Advisory chat streaming with risk-tier UI
- Conversation sidebar with tenant-isolated management
- CSV export with injection sanitisation
- Emergency escalation dialog

## Containers Rebuilt

- `arbor-backend` (Python/FastAPI)
- `arbor-frontend` (Next.js)

## Verification

- All 5 containers: HEALTHY
- Health endpoint: 200 OK
- Frontend: 200 OK
- SSL: Active (Let's Encrypt via Caddy)

## Environment

- Server: EC2 t2.medium (i-0632bfeef01ee415b)
- Region: ap-southeast-1 (Singapore)
- URL: https://arbor.terrene.foundation
