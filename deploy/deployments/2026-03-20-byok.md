# Deployment: 2026-03-20 — BYOK API Keys

> **Historical record (annotated 2026-05-07).** This file references `docker-compose.prod.yml`, which no longer exists — the GCE/compose deploy path was fictional per project owner (2026-05-07). The **commits + functional change** are authoritative; the **deploy mechanics** below describe an environment that never existed in that form. Current deploy reality lives in `deploy/deployment-config.md` and `.claude/skills/project/k8s-deploy.md`.

## Commits

- `748d135` — chore: sync COC artifacts from template
- `6daef6e` — feat: BYOK API keys with budget-capped default + Ollama/DGX support
- `778a1b2` — docs: BYOK analysis, plans, user flows, red team validation
- `e4cbc9f` — fix: correct useAuth import path
- `bbf9f61` — fix: use correct ButtonVariant "outlined"

## What Changed

- 37 new/modified source files (10 backend services, 3 frontend, 9 test files)
- 9 new API endpoints (6 company, 3 user)
- AI Configuration settings page at /settings/ai
- Budget tracking with $5/month default cap
- Multi-provider BYOK (OpenAI, Anthropic, Gemini, DeepSeek, Mistral)
- Ollama/DGX endpoint configuration
- Per-user personal API keys
- Key rotation CLI
- 165 tests (156 unit + 9 regression)

## Server Changes

- Added `LLM_KEY_ENCRYPTION_KEY` to .env.prod
- Added `OPENAI_PROD_MODEL=gpt-5-mini-2025-08-07`
- Updated `DEFAULT_LLM_MODEL=gpt-5-mini-2025-08-07`
- Added `LLM_KEY_ENCRYPTION_KEY` and `OPENAI_PROD_MODEL` to docker-compose.prod.yml

## Verification

- Backend health: healthy (all 3 workflows)
- Frontend: 200 OK
- AI Settings page: 200 OK at /settings/ai

## Issues During Deploy

- Frontend build failed twice:
  1. `useAuth` imported from `@/hooks/useAuth` instead of `@/contexts/AuthContext`
  2. `variant="outline"` should be `variant="outlined"` per AppButton types
- Both fixed with hotfix commits
