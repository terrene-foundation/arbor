# Deployment: 2026-03-13 v1.0.0

> **Historical record (annotated 2026-05-07).** This file claims an AWS EC2 deployment at `arbor.terrene.foundation` with specific instance/IP/security-group IDs. The current production environment is the DGX K8s cluster at `arbor.aitelab.net` and project owner has confirmed the GCE/AWS path never existed in current form. The **commits + functional change** are authoritative; the **infrastructure details** below describe an environment that does not match current reality. Current deploy reality lives in `deploy/deployment-config.md` and `.claude/skills/project/k8s-deploy.md`.

## Summary

Initial production deployment of Arbor HR Advisory Platform to AWS.

## Infrastructure Provisioned

- EC2 instance `i-0632bfeef01ee415b` (t2.medium, reserved instance)
- Elastic IP `52.220.50.167`
- Security group `sg-08193a91dc92c2bfc` (arbor-kailash)
- Route53 A record: `arbor.terrene.foundation` → `52.220.50.167`

## Services Deployed

| Container      | Image                     | Status  |
| -------------- | ------------------------- | ------- |
| arbor-caddy    | caddy:2-alpine            | Healthy |
| arbor-backend  | custom (python:3.11-slim) | Healthy |
| arbor-frontend | custom (node:20-alpine)   | Healthy |
| arbor-postgres | pgvector/pgvector:pg16    | Healthy |
| arbor-redis    | redis:7-alpine            | Healthy |

## SSL Certificate

- Issuer: Let's Encrypt
- CN: arbor.terrene.foundation
- Expires: 2026-06-11
- Auto-renewal via Caddy

## Verification

- `https://arbor.terrene.foundation/` — 200 OK
- `https://arbor.terrene.foundation/health` — 200 OK
- All 5 containers healthy
- HTTPS working with valid certificate

## Issues Encountered

1. **Docker buildx missing**: Amazon Linux 2023 ships Docker without buildx plugin. Installed manually.
2. **Backend egg_base error**: Dockerfile COPY order wrong — `src/` not available during `pip install`. Fixed by copying `src/` and `README.md` before install.
3. **Next.js standalone missing**: `output: "standalone"` not set in `next.config.ts`. Added it.
4. **Backend unhealthy in production**: `DEBUG` env var not set, defaulting to `true` which blocks production mode. Added `DEBUG=false`.
5. **Private repo clone failed**: Used `rsync` instead of `git clone` to transfer code to server.

## Cost

- EC2: $0/month (reserved instance)
- EIP: $0 (attached to running instance)
- Route53: ~$0.50/month
- **Total: < $5/month**

## Deployed By

Claude Code (automated deployment)

## Git Commits

- `469a457` — Full platform codebase
- `7cb024d` — Production deployment config
- `745ab2a` — Deployment fixes
